{
  "name": "api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev --port 8787 --experimental-vectorize-bind-to-prod",
    "build": "tsc",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply mersal-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply mersal-db --remote"
  },
  "dependencies": {
    "hono": "^4.4.2",
    "pdfjs-dist": "^3.11.174",
    "unpdf": "^1.6.2"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240529.0",
    "typescript": "^5.4.5",
    "wrangler": "^3.57.1"
  }
}
